# from django.db import models

# # Create your models here.
# class FileUploadModel(models):
#     file = models.FileField(blank=True, default="")
#     created_at = models.DateTimeField(null=True, auto_now_add=True)
